# Local-only PostgreSQL settings. Replace every placeholder before starting Compose.
POSTGRES_USER=REPLACE_ME
POSTGRES_PASSWORD=REPLACE_ME
POSTGRES_DB=loadplanner
DATABASE_URL=postgresql://REPLACE_ME:REPLACE_ME@db:5432/loadplanner
